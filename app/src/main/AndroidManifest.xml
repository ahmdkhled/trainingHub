<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.traininghub">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activities.ProfileActivity"></activity>
        <activity
            android:name=".view.activities.RegistrationActivity"
            android:exported="true" />
        <activity android:name=".view.activities.AllCoursesActivity" />
        <activity
            android:name=".view.activities.ReviewsActivity"
            android:exported="true"
            android:theme="@style/ReviewsActivityTheme" />
        <activity android:name=".view.activities.DetailActivity" />
        <activity android:name=".view.activities.MainActivity" />
        <activity
            android:name=".view.activities.SplashActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>